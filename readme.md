# URL shortener (Docker + Redis)
 
this project is a simple url shortener build using:
- Flask (python)
- redis (database)
- Docker & Docker Compose 
 
## Run project 
docker build -t url-app .

docker-compose up
docker clean version

